import AppointmentError from '../../components/email-actions/AppointmentError';

const AppointmentErrorPage = () => {
    return (
        <div className="pt-24 pb-12 min-h-[70vh]">
            <AppointmentError />
        </div>
    );
};

export default AppointmentErrorPage;
