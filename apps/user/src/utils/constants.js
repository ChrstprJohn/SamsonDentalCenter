/**
 * Clinic and Application configuration constants for the Patient Portal.
 * Shared across the user application to ensure consistent enforcement of rules.
 */
export const CLINIC_CONFIG = {
    MAX_DEPENDENTS_PER_USER: 10,
    MAX_ACTIVE_APPOINTMENTS_PER_ACCOUNT: 3,
    MAX_ACTIVE_APPOINTMENTS_PER_INDIVIDUAL: 3,
};
